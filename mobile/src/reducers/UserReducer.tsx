export const initialState = {
    favorites: [],
    appointments: []
}

export const UserReducer = (state: any, action: any) => {
    switch(action.type) {
        default:
            return state;
    }
}